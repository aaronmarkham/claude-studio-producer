# System Requirements
# FFmpeg must be installed separately for video/audio processing
# Install: brew install ffmpeg (macOS), apt install ffmpeg (Linux), choco install ffmpeg (Windows)
# See: https://ffmpeg.org/download.html

# Core dependencies
anthropic>=0.34.0
strands-agents>=0.1.0

# Async & utilities
aiohttp>=3.9.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# CLI
click>=8.1.0
rich>=13.0.0

# Video providers
lumaai>=0.1.0
openai>=1.0.0

# Audio providers
elevenlabs>=1.0.0

# Text analysis (vocabulary complexity for training pipeline)
textstat>=0.7.0
rouge-score>=0.1.2
numpy>=1.24.0

# Document ingestion
pymupdf>=1.24.0

# Templates (for server)
jinja2>=3.1.0
